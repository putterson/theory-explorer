const viewhints =
	(state = {
	  fret_start: 0,
	  fret_end: 15,
	  width: '100%',
	  height: '200px'}, action) => {
	    switch (action.type) {
	      default:
	      return state
	    }
	  }

export default viewhints
